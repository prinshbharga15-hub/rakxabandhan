{
  "name": "raksha-bandhan-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "start": "cd server && node server.js"
  }
}
